.outer(block="inline")
  | Vue: {{message}}
  script: :lsc
    do
      pkg:
        dependencies: [
          "https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"
        ]
      init: ({root, context}) ->
        app = new Vue do
          el: root
          data: message: "hello world!"

